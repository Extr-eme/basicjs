// 9. Write a program that iterates over an array of names and logs
//  each name to the console with its index. Hint.forEach()

let names=['ram','shyam','hari']
names.forEach((names,index) => {
    console.log(`name:${names} index:${index}`)
});
